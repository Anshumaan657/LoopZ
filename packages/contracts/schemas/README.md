# JSON Schema exports

This directory will contain generated JSON Schema representations of the versioned runtime contracts before external integrations are added.

Do not hand-edit generated schemas. The source definitions live in `src/`.
