# Artifacts feature

Creates or reuses the real browser-local run for a confirmed contract version, then previews, copies, and downloads the exact selected renderer output. Codex is optimized; Universal is explicitly a compatibility mode.
