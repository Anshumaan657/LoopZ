# Evaluation cases

This directory will contain controlled paired tasks:

```text
Baseline: rough request → Codex
Product:  rough request → LoopZ contract → Codex
```

Every case must pin the repository state, agent/model version, permissions, time or turn limit, and evaluator criteria.
